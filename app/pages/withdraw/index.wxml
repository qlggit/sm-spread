<import src="../common/page-data.wxml"></import>
<view class="height-100-100 position-relative">
    <view class="pl-20 pr-20">
        <view class="pt-10 pb-10 clearfix lh-btn ">我的余额：{{balance}} <button class="float-right" type="primary"
                                                                            bind:tap="navigateTo"
                                                                            data-link="/pages/withdraw/info"
        >申请提现</button></view>
        <view class="pt-10 pb-10">提现记录</view>
        <template is="searchForm" data="{{...searchData}}"></template>
        <template wx:if="{{tableDataAble}}" is="tableData" data="{{searchData:searchData,pageData:pageData,notBtn:150,tableScrollLeft:tableScrollLeft,showMainWidth:showMainWidth}}"></template>
    </view>

</view>
